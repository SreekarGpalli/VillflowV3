# VillFlow — agent entry point

You are one of three coding agents building VillFlow (Windows push-to-talk dictation, Rust + Tauri v2).

1. Read `CONTRACTS.md` fully — it is the authoritative spec and wins over everything else.
2. Your brief: if you are GrokBuild → `prompts/grokbuild.md`; Antigravity → `prompts/antigravity.md`; opencode → `prompts/opencode.md`. Read ONLY your own brief.
3. Obey CONTRACTS §4 (crate ownership) and §16 (process rules). Build check: `cargo build --workspace` on Windows MSVC.
4. `My Requirements.md` and `Wispr Flow — ….md` are the original human specs — background only; CONTRACTS.md supersedes them.
